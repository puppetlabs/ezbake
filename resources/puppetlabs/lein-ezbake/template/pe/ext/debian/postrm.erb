#!/bin/sh -e

case "$1" in
    purge)
        # Remove <%= EZBake::Config[:real_name] %> log files
        rm -rf /var/log/puppetlabs/<%= EZBake::Config[:real_name] %>
        ;;
    remove|upgrade|failed-upgrade|abort-install|abort-upgrade|disappear)
        ;;

    *)
        echo "postrm called with unknown argument \`$1'" >&2
        exit 1

esac

exit 0
