/var/log/puppetlabs/<%= EZBake::Config[:real_name] -%>/*.log {
    weekly
    missingok
    rotate 12
    compress
    copytruncate
    notifempty
    create 640 <%= EZBake::Config[:user] -%> <%= EZBake::Config[:group] %>
}
