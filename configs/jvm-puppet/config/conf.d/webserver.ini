[webserver]

client-auth = want
ssl-host = 0.0.0.0
ssl-port = 8081

# TODO: these are the paths on default el6 OSS puppet install;
# these can't be hard-coded like this in the long run
ssl-cert = /etc/puppet/conf/ssl/certs/localhost.pem
ssl-key = /etc/puppet/conf/ssl/private_keys/localhost.pem
ssl-ca-cert = /etc/puppet/conf/ssl/certs/ca.pem